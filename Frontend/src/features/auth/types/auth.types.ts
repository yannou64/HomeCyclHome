export type LoginPayload = {
    email: string;
    password: string;
};

export type RegisterPayload = {
    nom: string;
    prenom: string;
    email: string;
    password: string;
    telephone: string;
};
